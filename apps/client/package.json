{
  "name": "@game/client",
  "version": "0.1.0",
  "private": true,
  "description": "Cliente del juego (navegador). Three.js + Vite.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.14.0",
    "@game/config": "*",
    "@game/shared": "*",
    "colyseus.js": "^0.16.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "happy-dom": "^20.14.5",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
