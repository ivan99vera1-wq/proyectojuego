{
  "name": "@game/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Código compartido cliente/servidor: protocolo, tipos, matemáticas, reglas de juego puras.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@game/config": "*",
    "@dimforge/rapier3d-compat": "^0.14.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
