{
  "name": "@game/config",
  "version": "0.1.0",
  "private": true,
  "description": "ÚNICO punto de configuración del juego: nombre, marca, armas, personajes, mapas, reglas, red.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
